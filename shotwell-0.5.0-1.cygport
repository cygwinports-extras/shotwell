DESCRIPTION=
HOMEPAGE="http://yorba.org/shotwell/"
SRC_URI="http://yorba.org/download/shotwell/${PV[1]}.${PV[2]}/${P}.tar.bz2"
PATCH_URI="0.5.0-cygwin.patch
           0.5.0-vala08.patch"

src_compile() {
	lndirs
	cd ${B}
	./configure --prefix=/usr --disable-schemas-install --disable-desktop-update --disable-icon-update
	cygmake
}
